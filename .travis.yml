language: csharp
solution: Natasha.sln
dist: xenial
dotnet: 3.0.100
mono: none

branches:
  only:
  - master

os:
  - linux
  - osx

osx_image: xcode9.1
before-script:

script:
 - dotnet restore Build.csproj
 - dotnet flubu Rebuild.Server -apiKey=${Nuget_Api_Key}
